# Exercises several recent blocklists plus edge cases and large delta imports.
: "${LOADTEST_PROFILE_DESCRIPTION:=Mixed newest-window workload with three large blocklists, empty and expired alerts, and 1,000-25,000-decision delta blocklists.}"
: "${LOADTEST_ALERTS:=10000}"
: "${LOADTEST_DECISIONS:=500000}"
: "${LOADTEST_BLOCKLIST_SIZES:=125000,100000,60000}"
: "${LOADTEST_EMPTY_ALERTS:=1000}"
: "${LOADTEST_EXPIRED_ALERTS:=500}"
: "${LOADTEST_EXPIRING_SOON_DECISIONS:=8000}"
: "${LOADTEST_REFRESH_ALERTS:=3}"
: "${LOADTEST_REFRESH_DECISIONS:=0}"
: "${LOADTEST_REFRESH_DECISIONS_MIN_PER_ALERT:=1000}"
: "${LOADTEST_REFRESH_DECISIONS_MAX_PER_ALERT:=25000}"
: "${LOADTEST_REFRESH_DECISION_ORIGINS:=lists,CAPI}"
: "${LOADTEST_ACTIVE_DECISION_RATIO:=1}"
: "${CONFIG_CROWDSEC_ALERT_FILTERS_INCLUDE_CAPI:=true}"
: "${CONFIG_CROWDSEC_SYNC_LOOKBACK:=5d}"
: "${CONFIG_CROWDSEC_SYNC_REFRESH_INTERVAL:=1m}"
: "${CONFIG_CROWDSEC_SYNC_RECONCILE_ACTIVE_INTERVAL:=1m}"
